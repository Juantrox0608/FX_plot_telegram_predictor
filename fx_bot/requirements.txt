# === FX Bot V2 — dependencias Python ===
# Versiones FIJAS a las probadas con Python 3.14.3 (para que el VPS quede idéntico).
# El calendario económico usa urllib (estándar), no requiere 'requests'.

# Broker / datos / órdenes (solo Windows)
MetaTrader5==5.0.5735

# Bot de Telegram (API async moderna) + job-queue para el loop programado
python-telegram-bot[job-queue]==22.8

# IA (CPU es suficiente para el GRU)
torch==2.13.0

# Datos e indicadores
pandas==3.0.0
numpy==2.4.2
pyarrow==25.0.0

# Config / secretos
python-dotenv==1.2.2
